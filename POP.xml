<?xml version="1.0" encoding="utf-8"?>
<user_transforms>
	<transform_def name="POP">
		<node name="input_params">
			<real name="scale">1</real>
			<real name="POP_p">0.0</real>
			<real name="POP_o">0</real>

		</node>

		<node name="internal_params">
			<real name="scale" />
			<real name="p" />
			<real name="o" />
		</node>

		<int name="num_unit_randoms">1</int>

		<string name="winter_init_function">
			<![CDATA[
					POP_init_result(scale, POP_p, POP_o)
			]]>
		</string>

		<string name="winter_eval_function">
			<![CDATA[
				let
					z = complex(p_in.x, p_in.y) / scale
					oo = floor(unit_rnd_0 * (360 / o)) * (o * (pi() / 180))
					po = complex(p, 0)
					ro = complex(cos(oo), sin(oo))
					p0 = (z + po) / (z * po + 1)
					pr = p0 * ro
					f = (pr - po) / (pr * -po + 1)
				in
					toVec2(f) * scale
			]]>
		</string>
	</transform_def>


</user_transforms>
