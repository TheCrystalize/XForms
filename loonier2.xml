<?xml version="1.0" encoding="utf-8"?>
<user_transforms>
	<transform_def name="loonier2">
		<node name="input_params">
			<real name="loonier2">1</real>
			<real name="loonier2_n">1</real>
			<real name="loonier2_radius">1</real>
			<real name="loonier2_x">1</real>
			<real name="loonier2_y">1</real>
		</node>

		<node name="internal_params">
			<real name="scale" />
			<real name="n" />
			<real name="tpn" />
			<real name="rad" />
			<real name="x" />
			<real name="y" />
		</node>


		<string name="winter_init_function">
			<![CDATA[
				loonier2_init_result(loonier2, loonier2_n, twopi() / loonier2_n, loonier2_radius, loonier2_x, loonier2_y)
			]]>
		</string>

		<string name="winter_eval_function">
			<![CDATA[
				let
					r1 = length(p_in)
					ang = atan2(p_in.y, p_in.x) + pi()
					r2 = (cos(floor(0.5 + ang / tpn) * tpn - ang) * r1)
					r = if(rad >= 0, sqrt(rad / r1 - 1), 1 / sqrt(r1 / rad - 1))
				in
					p_in * r * scale
			]]>
		</string>
	</transform_def>


</user_transforms>
